import { Callout } from 'nextra/components'
import Image from 'next/image'
import Link from 'next/link'

## HTTP 测试接口
<br></br>
<Link href="https://http.rw/" target="_blank" style={{ textDecoration: 'none', color: '#3A9FE8' }}>http.rw</Link>

这是一个使用 httpbin 的开源程序实现的 http 测试接口。由于目前这个程序比较老，套了cf本身效果如何还未可知，回头会逐步替换为 php 的。

打开网页就是个接口列表和测试工具，可以玩玩。当然，在命令行使用也毫无问题，如：

```shell copy"
curl http.rw/ip
```

这样就可以获取本机IP，大概就是这样。


